import 'package:flutter/material.dart';

class TodoMain extends StatelessWidget {
  const TodoMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print("TodoMain Called.");
    return const Placeholder();
  }
}
