import 'package:flutter/material.dart';

class TaskViewCompact extends StatelessWidget {
  const TaskViewCompact({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children: [
          Row(
            children: [

            ],
          )
        ],
      ),
    );
  }
}
